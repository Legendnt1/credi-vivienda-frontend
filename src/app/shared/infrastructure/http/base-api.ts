/**
 * Base Api que sirve como clase padre para las diferentes APIs.
 * No define métodos por sí misma, pero permite que las clases hijas
 * compongan instancias de endpoints específicos.
 */
export abstract class BaseApi {
}

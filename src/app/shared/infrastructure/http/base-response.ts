/**
 * Define una estructura base para las respuestas HTTP.
 */
export interface BaseResponse {
}

/**
 * Define una estructura base para los recursos con un identificador único.
 */
export interface BaseResource {
  /**
   * Identificador único del recurso.
   */
  id: number;
}
